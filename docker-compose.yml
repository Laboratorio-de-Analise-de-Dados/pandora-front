services:
  frontend:
    image: paulomoro/pandora-front:latest
    container_name: pandora_frontend
    expose:
      - "8080"
    networks:
      - pandora_net

networks:
  pandora_net:
    external: true
