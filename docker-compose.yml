services:
  frontend:
    image: paulomoro/pandora-front:latest
    container_name: pandora_frontend
    expose:
      - "80"
    networks:
      - pandora_net

networks:
  pandora_net:
    external: true
